const newsDatabase = [
            {
                id: 1,
                title: "رئیس‌جمهور کلمبیا در بالگرد هدف ترور قرار گرفت + جزئیات",
                category: "political",
                categoryFa: "سیاسی",
                content: " «گوستاوو پترو» رئیس‌جمهور کلمبیا اعلام کرد بالگردی که او در آن حضور داشته، به دلیل طرحی برای ترور وی، مجبور به تغییر مسیر شده است.وی در نشست دولتی که از طریق کانال یوتیوب نهاد ریاست‌جمهوری پخش می‌شد، گفت: «در جایی که قرار بود، فرود نیامدم، زیرا نگرانی‌هایی وجود داشت مبنی بر اینکه ممکن است به بالگردی که فرزندانم نیز در آن حضور داشتند، شلیک شود.»",
                image: "https://cdn.donya-e-eqtesad.com/thumbnail/EXua9wUyVWmn/QHn8O9nsSzT8qCU7RegsN6Pbb5v74eEtbKeSOh05RaZCRCPo6kYZSEt7TZyzEhnm/X2fnnRE384FF.jpg",
                date: "۲ ساعت پیش",
                views: 1245,
                isFeatured: true
            },
            {
                id: 2,
                title: "بمب خبری درباره مبلغ کالابرگ برای ۱۴۰۵ / تکلیف روشن شد",
                category: "economic",
                categoryFa: "اقتصادی",
                content: " نایب رئیس کمیسیون اجتماعی مجلس شورای اسلامی تاکید کرد: حتما میزان کالابرگ در سال آینده باید افزایش یابد و شخص رئیس جمهور نیز تاکید داشتند که با تغییر قیمت ها رقم کالابرگ نیز تغییر خواهد کرد.",
                image: "https://cdn.khodroyek.ir/thumbnail/azaTrwhy7U5b/GK0QGt-rKNKlK72PI5N6PSLpuaPE4YjwRVpHs2uEeOUqam2qwiYf7d4f5Wudw48auqiceXcZCwkbb9vEtIwWx-MCvsVAx7deRYJJXQoo0PkkYcWfTiqmyIdMEO9CXMqJEHmoER0BiEcoxIf2faZtOQ,,/%DA%A9%D8%A7%D9%84%D8%A7%D8%A8%D8%B1%DA%AF%DB%B2.jpg",
                date: "۵ ساعت پیش",
                views: 892,
                isFeatured: true
            },
            {
                id: 3,
                title: "حمله کارشناس داوری به AFC: گل استقلال هیچ ایرادی نداشت/ آفساید گل آسانی خلاف خواسته فیفا بود",
                category: "sports",
                categoryFa: "ورزشی",
                content: "کارشناسان داوری پس از پایان دیدار استقلال و الحسین نقطه نظرات متفاوتی در رابطه با گل‌های مردود اعلام شده استقلال داشتند.",
                image: "https://media.khabarvarzeshi.com/d/2026/02/10/4/421420.jpg?ts=1770733632000",
                date: "۸ ساعت پیش",
                views: 2345,
                isFeatured: true
            },
            {
                id: 4,
                title: "شوخی عارف با وزیر ارشاد/ استخر سروش صحت پر نشد!",
                category: "cultural",
                categoryFa: "فرهنگی",
                content: "آخرین روز جشنواره‌ی فیلم فجر امسال نیز سرانجام فرا رسید. پس از یازده روز ماراتن سینمایی، اهالی رسانه و منتقدان آخرین آثار حاضر در جشنواره را تماشا کردند تا کم‌کم خود را برای مراسم اختتامیه‌ای که قرار است در تالار وحدت برگزار شود، آماده کنند.",
                image: "https://newsmedia.tasnimnews.ir/Tasnim/Uploaded/Image/1404/11/22/140411221250140035619384.jpg",
                date: "۱ ساعت پیش",
                views: 267,
                isFeatured: false
            },
            {
                id: 5,
                title: "راهنمای خرید گوشی موبایل – بهمن ماه ۱۴۰۴",
                category: "technology",
                categoryFa: "علم و تکنولوژی",
                content: "شاید اگر چند سال پیش برای خرید گوشی هوشمند سری به بازار می‌زدید، طی چند ساعت بهترین گوشی را می‌خریدید اما حالا انتخاب بسیار سخت‌تر شده و هر ماه چند گوشی جدید وارد بازار می‌شوند.",
                image: "https://static.digiato.com/digiato/2025/02/mobilephone-3-910x600.jpg.webp",
                date: "۳ ساعت پیش",
                views: 1234,
                isFeatured: false
            },
            {
                id: 6,
                title: "کمیسیون اروپا مورد حمله سایبری قرار گرفت؛ سرقت اطلاعات برخی کارکنان",
                category: "technology",
                categoryFa: "علم و تکنولوژی",
                content: "کمیسیون اروپا به‌تازگی اعلام کرده که زیرساخت‌های مدیریت دستگاه‌های موبایل (MDM) این نهاد مورد حمله سایبری قرار گرفته است.",
                image: "https://static.digiato.com/digiato/2026/02/EU1-910x600.jpg.webp",
                date: "۶ ساعت پیش",
                views: 987,
                isFeatured: false
            },
            {
                id: 7,
                title: "محورهای گفتگوی تلفنی امیر قطر و ترامپ",
                category: "international",
                categoryFa: "بین‌الملل",
                content: "دفتر امیر قطر با انتشار بیانیه‌ای اعلام کرد: شیخ تمیم بن حمد امیر قطر و دونالد ترامپ رئیس جمهور آمریکا، تلفنی درباره تلاشهای جاری برای کاهش تنش در منطقه و تقویت امنیت و صلح گفتگو کردند.",
                image: "https://media.mehrnews.com/d/2026/02/11/3/5900412.jpg?ts=1770802439135",
                date: "۱۰ ساعت پیش",
                views: 654,
                isFeatured: false
            },
            {
                id: 8,
                title: "۴۰۰ کیلو اورانیوم چه حجمی دارد؟",
                category: "economic",
                categoryFa: "اقتصادی",
                content: "بررسی‌های فنی نشان می‌دهد ۴۰۰ کیلوگرم اورانیوم در صورت تبدیل به هگزا فلوراید اورانیوم (UF₆)، که شکل رایج آن در فرآیند غنی‌سازی است، حجمی در حدود ۱۱۶ لیتر اشغال می‌کند.",
                image: "https://media.tahlilbazaar.com/d/2026/02/11/3/259304.jpg?ts=1770780331000",
                date: "۴ ساعت پیش",
                views: 1789,
                isFeatured: false
            },
            {
                id: 9,
                title: "کامپوزیت دندان چیست؟ قیمت کامپوزیت دندان در تهران ۱۴۰۴",
                category: "economic",
                categoryFa: "اقتصادی",
                content: "یکی از مهم‌ترین عوامل ظاهری تاثیرگذار بر اعتمادبه‌نفس و ارتباطات اجتماعی، زیبایی لبخند است. در علم دندانپزشکی، برای اصلاح طرح لبخند روش‌های مختلفی وجود دارد.",
                image: "https://media.tahlilbazaar.com/d/2026/02/11/3/259313.jpg?ts=1770787629000",
                date: "۷ ساعت پیش",
                views: 1123,
                isFeatured: false
            },
            {
                id: 10,
                title: "تغییر نظر ترامپ درباره موارد مذاکرات",
                category: "political",
                categoryFa: "سیاسی",
                content: "باراک راوید خبرنگار اسرائیلی در مصاحبه با کانال ۱۲ رژیم صهیونیستی مدعی شد: ترامپ به من گفت اگر نتوانند درباره برنامه هسته‌ای ایران به یک توافق دیپلماتیک برسند، آماده است دست به اقدام نظامی بزند.",
                image: "https://cdn.namehnews.com/thumbnail/cd9kxQkhRRmc/0KspN65RzgYFHFrusS9hOc_4F_1qGBmyi4CsYL-GywANxdMndkbfwc1zmTP6v02xhSDhHz_L_2TELcBJh1qN451HU_o3hO6h/%D8%AA%D8%B1%D8%A7%D9%85%D9%BE.jpg",
                date: "۲ ساعت پیش",
                views: 2456,
                isFeatured: false
            }
        ];

        const categoryIcons = {
            'political': 'fas fa-landmark',
            'economic': 'fas fa-chart-line',
            'cultural': 'fas fa-theater-masks',
            'sports': 'fas fa-futbol',
            'international': 'fas fa-globe-asia',
            'technology': 'fas fa-microchip',
            'lifestyle': 'fas fa-heart',
            'all': 'fas fa-newspaper'
        };

        const categoryNames = {
            'political': 'اخبار سیاسی',
            'economic': 'اخبار اقتصادی',
            'cultural': 'اخبار فرهنگی',
            'sports': 'اخبار ورزشی',
            'international': 'اخبار بین‌الملل',
            'technology': 'علم و تکنولوژی',
            'lifestyle': 'سبک زندگی',
            'all': 'همه اخبار'
        };

        let currentCategory = 'all';
        let currentSearchQuery = '';
        let isSearchActive = false;

        const navLinks = document.querySelectorAll('.nav-link');
        const categoryLinks = document.querySelectorAll('.category-link');
        const categoryTitle = document.getElementById('categoryTitle');
        const categoryName = document.getElementById('categoryName');
        const categoryIcon = document.getElementById('categoryIcon');
        const searchInput = document.getElementById('searchInput');
        const searchButton = document.getElementById('searchButton');
        const searchResults = document.getElementById('searchResults');
        const resultsCount = document.getElementById('resultsCount');
        const searchResultsContainer = document.getElementById('searchResultsContainer');
        const featuredGrid = document.getElementById('featuredGrid');
        const newsItemsContainer = document.getElementById('newsItemsContainer');
        const trendingNews = document.getElementById('trendingNews');
        const categoriesList = document.getElementById('categoriesList');
        const goToHome = document.getElementById('goToHome');
        const goToHomeFooter = document.getElementById('goToHomeFooter');

        function init() {
            updateDateTime();
            loadCategories();
            loadTrendingNews();
            displayNews('all');
            setInterval(updateDateTime, 60000);
            setupEventListeners();
            initTickerAnimation();
        }

        function updateDateTime() {
            const now = new Date();
            const options = { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric', hour: '2-digit', minute: '2-digit' };
            const farsiDate = now.toLocaleDateString('fa-IR', options);
            const el = document.getElementById('current-date-time');
            if (el) el.textContent = farsiDate;
        }

        function initTickerAnimation() {
            const tickerTrack = document.getElementById('tickerTrack');
            if (!tickerTrack) return;
            const breakingTitles = newsDatabase.slice(0, 6).map(n => `<span class="ticker-item">${n.title}</span>`).join('<span class="ticker-sep">◆</span>');
            tickerTrack.innerHTML = breakingTitles + '<span class="ticker-sep">◆</span>' + breakingTitles;
        }

        function setupEventListeners() {
            navLinks.forEach(link => {
                link.addEventListener('click', function(e) {
                    e.preventDefault();
                    const category = this.getAttribute('data-category');
                    navLinks.forEach(l => l.classList.remove('active'));
                    this.classList.add('active');
                    isSearchActive = false;
                    searchInput.value = '';
                    searchResults.classList.add('hidden');
                    if (category === 'all') {
                        categoryTitle.classList.add('hidden');
                        displayNews('all');
                    } else {
                        showCategory(category);
                    }
                });
            });

            categoryLinks.forEach(link => {
                link.addEventListener('click', function(e) {
                    e.preventDefault();
                    const category = this.getAttribute('data-category');
                    navLinks.forEach(l => { l.classList.remove('active'); if (l.getAttribute('data-category') === category) l.classList.add('active'); });
                    isSearchActive = false;
                    searchInput.value = '';
                    searchResults.classList.add('hidden');
                    showCategory(category);
                });
            });

            searchButton.addEventListener('click', performSearch);
            searchInput.addEventListener('keypress', function(e) { if (e.key === 'Enter') performSearch(); });

            goToHome.addEventListener('click', function(e) { e.preventDefault(); goToHomePage(); });
            if (goToHomeFooter) goToHomeFooter.addEventListener('click', function(e) { e.preventDefault(); goToHomePage(); });
        }

        function goToHomePage() {
            navLinks.forEach(l => { l.classList.remove('active'); if (l.getAttribute('data-category') === 'all') l.classList.add('active'); });
            isSearchActive = false;
            searchInput.value = '';
            searchResults.classList.add('hidden');
            categoryTitle.classList.add('hidden');
            displayNews('all');
        }

        function showCategory(category) {
            currentCategory = category;
            isSearchActive = false;
            categoryName.textContent = categoryNames[category];
            categoryIcon.className = categoryIcons[category];
            categoryTitle.classList.remove('hidden');
            displayNews(category);
        }

        function performSearch() {
            const query = searchInput.value.trim();
            if (query === '') { alert('لطفا عبارتی برای جستجو وارد کنید'); return; }
            currentSearchQuery = query;
            isSearchActive = true;
            categoryTitle.classList.add('hidden');
            searchResults.classList.remove('hidden');
            displaySearchResults(query);
        }

        function displayNews(category) {
            let filteredNews = category === 'all' ? newsDatabase : newsDatabase.filter(n => n.category === category);
            displayFeaturedNews(filteredNews.filter(n => n.isFeatured));
            displayLatestNews(filteredNews.filter(n => !n.isFeatured));
        }

        function displayFeaturedNews(newsList) {
            featuredGrid.innerHTML = '';
            if (newsList.length === 0) { featuredGrid.innerHTML = '<p class="no-news">خبری برای نمایش وجود ندارد.</p>'; return; }
            newsList.forEach((news, idx) => {
                const isLarge = idx === 0;
                const newsCard = document.createElement('div');
                newsCard.className = `featured-card ${isLarge ? 'large' : ''}`;
                newsCard.setAttribute('data-id', news.id);
                newsCard.innerHTML = `
                    <div class="featured-img">
                        <img src="${news.image}" alt="${news.title}" loading="lazy">
                        <div class="img-overlay"></div>
                        ${isLarge ? `<div class="large-card-content">
                            <span class="news-category-badge" data-category="${news.category}">${news.categoryFa}</span>
                            <h2 class="large-title">${news.title}</h2>
                            <div class="news-meta-inline">
                                <span><i class="far fa-clock"></i> ${news.date}</span>
                                <span><i class="far fa-eye"></i> ${news.views.toLocaleString()} بازدید</span>
                            </div>
                        </div>` : ''}
                    </div>
                    ${!isLarge ? `<div class="featured-content">
                        <span class="news-category-badge" data-category="${news.category}">${news.categoryFa}</span>
                        <h3>${news.title}</h3>
                        <p>${news.content.substring(0, 100)}...</p>
                        <div class="news-meta">
                            <span><i class="far fa-clock"></i> ${news.date}</span>
                            <span><i class="far fa-eye"></i> ${news.views.toLocaleString()}</span>
                        </div>
                    </div>` : ''}
                `;
                newsCard.querySelector('.news-category-badge').addEventListener('click', function(e) {
                    e.stopPropagation();
                    const cat = this.getAttribute('data-category');
                    navLinks.forEach(l => { l.classList.remove('active'); if (l.getAttribute('data-category') === cat) l.classList.add('active'); });
                    showCategory(cat);
                });
                newsCard.addEventListener('click', function() {
                    showNewsModal(news);
                });
                featuredGrid.appendChild(newsCard);
            });
        }

        function displayLatestNews(newsList) {
            newsItemsContainer.innerHTML = '';
            if (newsList.length === 0) { newsItemsContainer.innerHTML = '<p class="no-news">خبری برای نمایش وجود ندارد.</p>'; return; }
            newsList.forEach(news => {
                const newsItem = document.createElement('div');
                newsItem.className = 'news-item';
                newsItem.setAttribute('data-id', news.id);
                newsItem.innerHTML = `
                    <div class="news-item-img">
                        <img src="${news.image}" alt="${news.title}" loading="lazy">
                    </div>
                    <div class="news-item-content">
                        <span class="news-category-badge small" data-category="${news.category}">${news.categoryFa}</span>
                        <h4>${news.title}</h4>
                        <div class="news-meta">
                            <span><i class="far fa-clock"></i> ${news.date}</span>
                            <span><i class="far fa-eye"></i> ${news.views.toLocaleString()} بازدید</span>
                        </div>
                    </div>
                `;
                newsItem.querySelector('.news-category-badge').addEventListener('click', function(e) {
                    e.stopPropagation();
                    const cat = this.getAttribute('data-category');
                    navLinks.forEach(l => { l.classList.remove('active'); if (l.getAttribute('data-category') === cat) l.classList.add('active'); });
                    showCategory(cat);
                });
                newsItem.addEventListener('click', () => showNewsModal(news));
                newsItemsContainer.appendChild(newsItem);
            });
        }

        function displaySearchResults(query) {
            const results = newsDatabase.filter(n => n.title.includes(query) || n.content.includes(query) || n.categoryFa.includes(query));
            resultsCount.textContent = `${results.length} نتیجه برای «${query}» یافت شد`;
            searchResultsContainer.innerHTML = '';
            if (results.length === 0) {
                searchResultsContainer.innerHTML = `<div class="no-results"><i class="fas fa-search"></i><h3>نتیجه‌ای یافت نشد</h3><p>هیچ خبری با عبارت «${query}» پیدا نشد.</p></div>`;
                return;
            }
            results.forEach(news => {
                const item = document.createElement('div');
                item.className = 'news-item';
                item.innerHTML = `
                    <div class="news-item-img"><img src="${news.image}" alt="${news.title}" loading="lazy"></div>
                    <div class="news-item-content">
                        <span class="news-category-badge small" data-category="${news.category}">${news.categoryFa}</span>
                        <h4>${news.title}</h4>
                        <div class="news-meta">
                            <span><i class="far fa-clock"></i> ${news.date}</span>
                            <span><i class="far fa-eye"></i> ${news.views.toLocaleString()} بازدید</span>
                        </div>
                    </div>`;
                item.querySelector('.news-category-badge').addEventListener('click', function(e) {
                    e.stopPropagation();
                    const cat = this.getAttribute('data-category');
                    navLinks.forEach(l => { l.classList.remove('active'); if (l.getAttribute('data-category') === cat) l.classList.add('active'); });
                    showCategory(cat);
                });
                item.addEventListener('click', () => showNewsModal(news));
                searchResultsContainer.appendChild(item);
            });
        }

        function loadCategories() {
            categoriesList.innerHTML = '';
            const counts = {};
            newsDatabase.forEach(n => { counts[n.categoryFa] = (counts[n.categoryFa] || 0) + 1; });

            const allItem = document.createElement('li');
            allItem.innerHTML = `<a href="#" class="category-link ${currentCategory === 'all' ? 'active-category' : ''}" data-category="all"><span><i class="fas fa-newspaper"></i> همه اخبار</span><span class="category-count">${newsDatabase.length}</span></a>`;
            allItem.querySelector('.category-link').addEventListener('click', function(e) { e.preventDefault(); goToHomePage(); });
            categoriesList.appendChild(allItem);

            const catIconMap = { 'سیاسی': 'fa-landmark', 'اقتصادی': 'fa-chart-line', 'فرهنگی': 'fa-theater-masks', 'ورزشی': 'fa-futbol', 'بین‌الملل': 'fa-globe-asia', 'علم و تکنولوژی': 'fa-microchip' };
            for (const [catFa, count] of Object.entries(counts)) {
                let categoryKey = '';
                for (const [key, value] of Object.entries(categoryNames)) {
                    if (value.includes(catFa)) { categoryKey = key; break; }
                }
                const icon = catIconMap[catFa] || 'fa-circle';
                const item = document.createElement('li');
                item.innerHTML = `<a href="#" class="category-link ${currentCategory === categoryKey ? 'active-category' : ''}" data-category="${categoryKey}"><span><i class="fas ${icon}"></i> ${catFa}</span><span class="category-count">${count}</span></a>`;
                item.querySelector('.category-link').addEventListener('click', function(e) {
                    e.preventDefault();
                    const cat = this.getAttribute('data-category');
                    navLinks.forEach(l => { l.classList.remove('active'); if (l.getAttribute('data-category') === cat) l.classList.add('active'); });
                    showCategory(cat);
                });
                categoriesList.appendChild(item);
            }
        }

        function loadTrendingNews() {
            trendingNews.innerHTML = '';
            const trending = [...newsDatabase].sort((a, b) => b.views - a.views).slice(0, 5);
            trending.forEach((news, index) => {
                const item = document.createElement('li');
                item.innerHTML = `
                    <div class="trending-item">
                        <span class="trending-number">${index + 1}</span>
                        <div class="trending-content">
                            <a href="#" class="trending-link" data-id="${news.id}">${news.title}</a>
                            <span class="trending-views"><i class="far fa-eye"></i> ${news.views.toLocaleString()}</span>
                        </div>
                    </div>`;
                item.querySelector('.trending-link').addEventListener('click', function(e) { e.preventDefault(); showNewsModal(news); });
                trendingNews.appendChild(item);
            });
        }

        function showNewsModal(news) {
            const existing = document.getElementById('newsModal');
            if (existing) existing.remove();

            const modal = document.createElement('div');
            modal.id = 'newsModal';
            modal.className = 'news-modal';
            modal.innerHTML = `
                <div class="modal-overlay" id="modalOverlay"></div>
                <div class="modal-container">
                    <div class="modal-header">
                        <span class="news-category-badge" data-category="${news.category}">${news.categoryFa}</span>
                        <button class="modal-close" id="modalClose"><i class="fas fa-times"></i></button>
                    </div>
                    <img src="${news.image}" alt="${news.title}" class="modal-img">
                    <div class="modal-body">
                        <h2 class="modal-title">${news.title}</h2>
                        <div class="modal-meta">
                            <span><i class="far fa-clock"></i> ${news.date}</span>
                            <span><i class="far fa-eye"></i> ${news.views.toLocaleString()} بازدید</span>
                        </div>
                        <p class="modal-content">${news.content}</p>
                    </div>
                </div>`;
            document.body.appendChild(modal);
            setTimeout(() => modal.classList.add('active'), 10);

            document.getElementById('modalClose').addEventListener('click', () => closeModal(modal));
            document.getElementById('modalOverlay').addEventListener('click', () => closeModal(modal));
            document.body.style.overflow = 'hidden';
        }

        function closeModal(modal) {
            modal.classList.remove('active');
            setTimeout(() => { modal.remove(); document.body.style.overflow = ''; }, 300);
        }

        init();